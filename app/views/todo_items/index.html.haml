-if @tag
	%h2= ": #{@tag.name}"

#add
	%h4 New Item
	%p
		-form_for :todo_item, @todoItem, :url => { :action => "create" } do |f| 
			= f.text_field :tag_name 
			= hidden_field_tag :tag, { :value => @tag.tag_id } if @tag 
			= submit_tag 'Add' 

#list
	%ul.items
		= render @todoItems
